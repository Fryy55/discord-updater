# Discord Updater

just a small updater script for my system, where discord is installed at /opt/discord via a tar.gz archive, also includes a vencord installer

add symlinks to `vencord` and `discord-update` to some PATH dir or add the repo dir to PATH if you want to use it anywhere but ig you know that already
